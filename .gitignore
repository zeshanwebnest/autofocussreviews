node_modules/
.next/

# Secrets — never commit these. .env holds the Supabase service-role key,
# which grants full read/write access to the database.
.env
.env.local
.env*.local
!.env.example

npm-debug.log*
